readme
  input[number]
    input[text], but no number software keyboard
    limitations: '0.' => '0', no selectionStart, etc.
  unconventional decision
    no transpilation: template literals, no webpack/preact-cli
    functional style: lambdas
    meiosis for state
    typescript as an external tool
  grammar
